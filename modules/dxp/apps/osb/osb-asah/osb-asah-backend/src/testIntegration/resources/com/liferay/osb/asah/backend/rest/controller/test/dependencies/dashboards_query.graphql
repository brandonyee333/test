query Dashboard($channelId: String, $keywords: String, $size: Int!, $sort: Sort!, $start: Int!) {
	dashboards(channelId: $channelId, keywords: $keywords, size: $size, sort: $sort, start: $start) {
		dashboards {
			assetTitle
			id
			modifiedDate
			modifiedByUserName
		}
		total
	}
}
