mutation HideBlockedEventDefinitions($blockedEventDefinitionIds: [String]!) {
    hideBlockedEventDefinitions(blockedEventDefinitionIds: $blockedEventDefinitionIds) {
        hidden
        id
    }
}