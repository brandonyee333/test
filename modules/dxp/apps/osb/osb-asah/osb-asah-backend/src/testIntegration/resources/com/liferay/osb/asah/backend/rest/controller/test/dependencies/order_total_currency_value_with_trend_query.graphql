query OrderTotalCurrencyValue($channelId: String!, $rangeKey: Int) {
	orderTotalCurrencyValues(channelId: $channelId, rangeKey: $rangeKey) {
		currencyCode
		trend {
			percentage
			trendClassification
		}
		value
	}
}