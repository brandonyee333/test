<?xml version="1.0"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="EventAttribute.findDistinctAttributeValues">
		<![CDATA[
			SELECT
				MAX(eventDate) as lastSeenDate,
				value
			FROM
				EventAttribute
			WHERE
				EventAttribute.eventAttributeDefinitionId =
					:eventAttributeDefinitionId
			GROUP BY
				value
			ORDER BY
				lastSeenDate DESC
			LIMIT
				:size
		]]>
	</entry>
</properties>