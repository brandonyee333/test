<?xml version="1.0"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="EventAttribute.findDistinctAttributeValues">
		<![CDATA[
			SELECT
				MAX(EventAttribute.eventDate) as lastSeenDate,
				EventAttribute.value
			FROM
				EventAttribute
			WHERE
				EventAttribute.eventAttributeDefinitionId =
					:eventAttributeDefinitionId
			GROUP BY
				EventAttribute.value
			ORDER BY
				lastSeenDate DESC
			LIMIT
				:size
		]]>
	</entry>
</properties>