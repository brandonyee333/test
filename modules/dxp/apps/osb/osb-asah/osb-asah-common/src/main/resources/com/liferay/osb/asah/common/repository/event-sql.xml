<?xml version="1.0"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="Event.findDistinctAttributeValues">
		<![CDATA[
			SELECT
				MAX(Event.eventDate) as lastSeenDate,
				EventAttribute.attributeValue
			FROM
				Event
			JOIN
				EventAttribute ON
					Event.id = EventAttribute.eventId
			WHERE
				EventAttribute.eventAttributeDefinitionId =
					:eventAttributeDefinitionId
			GROUP BY
				EventAttribute.attributeValue
			ORDER BY
				lastSeenDate DESC
			LIMIT
				:size
		]]>
	</entry>
</properties>