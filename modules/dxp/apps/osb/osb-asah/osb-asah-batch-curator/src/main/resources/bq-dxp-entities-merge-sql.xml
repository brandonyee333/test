<?xml version="1.0"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="AccountEntryMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.accountentry` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					accountEntry.*,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'accountEntryId'
					) AS accountEntryId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'type'
					) AS accountEntryType,
					(
						SELECT
							SAFE_CAST(TIMESTAMP_MILLIS(CAST(value AS INT64)) AS TIMESTAMP)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'createDate'
					) AS createDate,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'defaultCPaymentMethodKey'
					) AS defaultCPaymentMethodKey,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'description'
					) AS description,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'domains'
					) AS domains,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'emailAddress'
					) AS emailAddress,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'logoId'
					) AS logoId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'name'
					) AS name,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'parentAccountEntryId'
					) AS parentAccountEntryId,
					TO_HEX(
						SHA256(
							CONCAT(accountEntry.projectId, '#', accountEntry.dataSourceId, '#', accountEntry.classPK)
						)
					) AS sha256HexId,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'status'
					) AS status,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'taxExemptionCode'
					) AS taxExemptionCode,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountEntry.fields)
						WHERE
							name = 'taxIdNumber'
					) AS taxIdNumber
				FROM
					`{projectId}.dxpentity` AS accountEntry
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS accountEntryId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.accountEntryId = accountEntry.classPK AND
					analyticsDeleteMessage.className = accountEntry.type
				WHERE
					(
						analyticsDeleteMessage.accountEntryId IS NOT NULL OR
						accountEntry.uploadDate >= '{previousRunDateString}'
					) AND
					accountEntry.type = 'com.liferay.account.model.AccountEntry'

			) AS staging
			ON
				staging.classPK = replica.accountEntryId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.defaultCPaymentMethodKey = staging.defaultCPaymentMethodKey,
					replica.description = staging.description,
					replica.domains = staging.domains,
					replica.emailAddress = staging.emailAddress,
					replica.logoId = staging.logoId,
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name,
					replica.parentAccountEntryId = staging.parentAccountEntryId,
					replica.status = staging.status,
					replica.taxExemptionCode = staging.taxExemptionCode,
					replica.taxIdNumber = staging.taxIdNumber,
					replica.type = staging.accountEntryType
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`accountEntryId`,
					`createDate`,
					`dataSourceId`,
					`defaultCPaymentMethodKey`,
					`description`,
					`domains`,
					`emailAddress`,
					`id`,
					`logoId`,
					`modifiedDate`,
					`name`,
					`parentAccountEntryId`,
					`projectId`,
					`taxExemptionCode`,
					`taxIdNumber`,
					`type`,
					`status`
				)
				VALUES (
					staging.accountEntryId,
					staging.createDate,
					staging.dataSourceId,
					staging.defaultCPaymentMethodKey,
					staging.description,
					staging.domains,
					staging.emailAddress,
					staging.sha256HexId,
					staging.logoId,
					staging.modifiedDate,
					staging.name,
					staging.parentAccountEntryId,
					staging.projectId,
					staging.taxExemptionCode,
					staging.taxIdNumber,
					staging.accountEntryType,
					staging.status
				)
		]]>
	</entry>
	<entry key="AccountGroupMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.accountgroup` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					accountGroup.*,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(accountGroup.fields)
						WHERE
							name = 'accountGroupId'
					) AS accountGroupId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountGroup.fields)
						WHERE
							name = 'type'
					) AS accountGroupType,
					(
						SELECT
							SAFE_CAST(TIMESTAMP_MILLIS(CAST(value AS INT64)) AS TIMESTAMP)
						FROM
							UNNEST(accountGroup.fields)
						WHERE
							name = 'createDate'
					) AS createDate,
					(
						SELECT
							SAFE_CAST(CAST(value AS STRING) AS BOOL)
						FROM
							UNNEST(accountGroup.fields)
						WHERE
							name = 'defaultAccountGroup'
					) AS defaultAccountGroup,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountGroup.fields)
						WHERE
							name = 'description'
					) AS description,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(accountGroup.fields)
						WHERE
							name = 'name'
					) AS name,
					TO_HEX(
						SHA256(
							CONCAT(accountGroup.projectId, '#', accountGroup.dataSourceId, '#', accountGroup.classPK)
						)
					) AS sha256HexId
				FROM
					`{projectId}.dxpentity` AS accountGroup
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS accountGroupId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.accountGroupId = accountGroup.classPK AND
					analyticsDeleteMessage.className = accountGroup.type
				WHERE
					(
						analyticsDeleteMessage.accountGroupId IS NOT NULL OR
						accountGroup.uploadDate >= '{previousRunDateString}'
					) AND
					accountGroup.type = 'com.liferay.account.model.AccountGroup'

			) AS staging
			ON
				staging.classPK = replica.accountGroupId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.defaultAccountGroup = staging.defaultAccountGroup,
					replica.description = staging.description,
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name,
					replica.type = staging.accountGroupType
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`accountGroupId`,
					`createDate`,
					`dataSourceId`,
					`defaultAccountGroup`,
					`description`,
					`id`,
					`modifiedDate`,
					`name`,
					`projectId`,
					`type`
				)
				VALUES (
					staging.accountGroupId,
					staging.createDate,
					staging.dataSourceId,
					staging.defaultAccountGroup,
					staging.description,
					staging.sha256HexId,
					staging.modifiedDate,
					staging.name,
					staging.projectId,
					staging.accountGroupType
				)
		]]>
	</entry>
	<entry key="ExpandoColumnMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.expandocolumn` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					expandoColumn.*,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(expandoColumn.fields)
						WHERE
							name = 'className'
					) AS className,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(expandoColumn.fields)
						WHERE
							name = 'columnId'
					) AS columnId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(expandoColumn.fields)
						WHERE
							name = 'dataType'
					) AS dataType,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(expandoColumn.fields)
						WHERE
							name = 'name'
					) AS name,
					TO_HEX(
						SHA256(
							CONCAT(expandoColumn.projectId, '#', expandoColumn.dataSourceId, '#', expandoColumn.classPK)
						)
					) AS sha256HexId
				FROM
					`{projectId}.dxpentity` AS expandoColumn
			 	LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS columnId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = expandoColumn.type AND
					analyticsDeleteMessage.columnId = expandoColumn.classPK AND
					analyticsDeleteMessage.dataSourceId = expandoColumn.dataSourceId AND
					analyticsDeleteMessage.projectId = expandoColumn.projectId
				WHERE
					(
						analyticsDeleteMessage.columnId IS NOT NULL OR
						expandoColumn.uploadDate >= '{previousRunDateString}'
					) AND
					expandoColumn.type = 'com.liferay.expando.kernel.model.ExpandoColumn'
			) AS staging
			ON
				staging.columnId = replica.columnId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.className = staging.className,
					replica.dataType = staging.dataType,
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`className`,
					`columnId`,
					`dataSourceId`,
					`dataType`,
					`id`,
					`modifiedDate`,
					`name`,
					`projectId`
				)
				VALUES (
					staging.className,
					staging.columnId,
					staging.dataSourceId,
					staging.dataType,
					staging.sha256HexId,
					staging.modifiedDate,
					staging.name,
					staging.projectId
				)
		]]>
	</entry>
	<entry key="ExpandoValueMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.expandovalue` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					dxpEntity.*,
					expandoField.*,
					TO_HEX(
						SHA256(
							CONCAT(dxpEntity.projectId, '#', dxpEntity.dataSourceId, '#', expandoField.columnId, '#', dxpEntity.classPK)
						)
					) AS sha256HexId
				FROM
					`{projectId}.dxpentity` AS dxpEntity, UNNEST(expandoFields) AS expandoField
			 	LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS entityId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = dxpEntity.type AND
					analyticsDeleteMessage.dataSourceId = dxpEntity.dataSourceId AND
					analyticsDeleteMessage.entityId = dxpEntity.classPK AND
					analyticsDeleteMessage.projectId = dxpEntity.projectId
				WHERE
					(
						analyticsDeleteMessage.entityId IS NOT NULL OR
						dxpEntity.uploadDate >= '{previousRunDateString}'
					) AND
					(
						dxpEntity.type = 'com.liferay.portal.kernel.model.Organization' OR
						dxpEntity.type = 'com.liferay.portal.kernel.model.User'
					)
			) AS staging
			ON
				staging.classPK = replica.classPK AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.columnId = replica.columnId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.value = staging.value
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`classPK`,
					`classType`,
					`columnId`,
					`dataSourceId`,
					`id`,
					`projectId`,
					`value`
				)
				VALUES (
					staging.classPK,
					staging.type,
					staging.columnId,
					staging.dataSourceId,
					staging.sha256HexId,
					staging.projectId,
					staging.value
				)
		]]>
	</entry>
	<entry key="GroupMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.group` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					group_.*,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(group_.fields)
						WHERE
							name = 'groupId'
					) AS groupId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(group_.fields)
						WHERE
							name = 'name'
					) AS name,
					TO_HEX(
						SHA256(
							CONCAT(group_.projectId, '#', group_.dataSourceId, '#', group_.classPK)
						)
					) AS sha256HexId
				FROM
					`{projectId}.dxpentity` AS group_
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS groupId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = group_.type AND
					analyticsDeleteMessage.dataSourceId = group_.dataSourceId AND
					analyticsDeleteMessage.groupId = group_.classPK AND
					analyticsDeleteMessage.projectId = group_.projectId
				WHERE
					(
						analyticsDeleteMessage.groupId IS NOT NULL OR
						group_.uploadDate >= '{previousRunDateString}'
					) AND
					group_.type = 'com.liferay.portal.kernel.model.Group'
			) AS staging
			ON
				staging.classPK = replica.groupId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`dataSourceId`,
					`groupId`,
					`id`,
					`modifiedDate`,
					`name`,
					`projectId`
				)
				VALUES (
					staging.dataSourceId,
					staging.groupId,
					staging.sha256HexId,
					staging.modifiedDate,
					staging.name,
					staging.projectId
				)
		]]>
	</entry>
	<entry key="OrganizationExpandoColumnMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.organization` AS replica
			USING (
				SELECT
					organization.*,
					ARRAY(SELECT unnestExpandoColumnIds
							FROM UNNEST(organization.expandoColumnIds) AS unnestExpandoColumnIds
							WHERE unnestExpandoColumnIds <> expandoColumnAnalyticsDeleteMessage.columnId) AS updatedExpandoColumnIds
					FROM (
						SELECT
							*,
							(
								SELECT
									SAFE_CAST(value AS STRING)
								FROM
									UNNEST(fields)
								WHERE
									name = 'classPK'
							) AS columnId,
							(
								SELECT
									SAFE_CAST(value AS STRING)
								FROM
									UNNEST(fields)
								WHERE
									name = 'className'
							) AS className
						FROM
							`{projectId}.dxpentity`
						WHERE
							type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage' AND
							uploadDate >= '{previousRunDateString}'
					) AS expandoColumnAnalyticsDeleteMessage
					INNER JOIN (
						SELECT
							*,
							(
							SELECT ARRAY(
								SELECT
									DISTINCT SAFE_CAST(columnId AS STRING)
								FROM
									UNNEST(expandoFields)
							)
						) AS expandoColumnIds,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'organizationId'
						) AS organizationId
						FROM
							`{projectId}.dxpentity`
						WHERE
							type = 'com.liferay.portal.kernel.model.Organization'
					) AS organization
					ON
						expandoColumnAnalyticsDeleteMessage.columnId in UNNEST(organization.expandoColumnIds) AND
						expandoColumnAnalyticsDeleteMessage.dataSourceId = organization.dataSourceId AND
						expandoColumnAnalyticsDeleteMessage.projectId = organization.projectId
					WHERE
						expandoColumnAnalyticsDeleteMessage.className = 'com.liferay.expando.kernel.model.ExpandoColumn'
			) AS staging
			ON
				staging.dataSourceId = replica.dataSourceId AND
				staging.modifiedDate = replica.modifiedDate AND
				staging.projectId = replica.projectId AND
				staging.organizationId = replica.organizationId
			WHEN MATCHED THEN
				UPDATE SET
					replica.expandoColumnIds = staging.updatedExpandoColumnIds
		]]>
	</entry>
	<entry key="OrganizationMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.organization` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					organization.*,
					(
						SELECT ARRAY(
							SELECT
								DISTINCT SAFE_CAST(columnId AS STRING)
							FROM
								UNNEST(organization.expandoFields)
						)
					) AS expandoColumnIds,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(organization.fields)
						WHERE
							name = 'name'
					) AS name,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(organization.fields)
						WHERE
							name = 'organizationId'
					) AS organizationId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(organization.fields)
						WHERE
							name = 'type'
					) AS organizationType,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(organization.fields)
						WHERE
							name = 'parentOrganizationId'
					) AS parentOrganizationId,
					TO_HEX(
						SHA256(
							CONCAT(organization.projectId, '#', organization.dataSourceId, '#', organization.classPK)
						)
					) AS sha256HexId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(organization.fields)
						WHERE
							name = 'treePath'
					) AS treePath
				FROM
					`{projectId}.dxpentity` AS organization
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS organizationId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = organization.type AND
					analyticsDeleteMessage.dataSourceId = organization.dataSourceId AND
					analyticsDeleteMessage.organizationId = organization.classPK AND
					analyticsDeleteMessage.projectId = organization.projectId
				WHERE
					(
						analyticsDeleteMessage.organizationId IS NOT NULL OR
						organization.uploadDate >= '{previousRunDateString}'
					) AND
					organization.type = 'com.liferay.portal.kernel.model.Organization'
			) AS staging
			ON
				staging.classPK = replica.organizationId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.expandoColumnIds = staging.expandoColumnIds,
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name,
					replica.type = staging.organizationType,
					replica.parentOrganizationId = staging.parentOrganizationId,
					replica.treePath = staging.treePath
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`dataSourceId`,
					`expandoColumnIds`,
					`id`,
					`modifiedDate`,
					`name`,
					`organizationId`,
					`parentOrganizationId`,
					`projectId`,
					`treePath`,
					`type`
				)
				VALUES (
					staging.dataSourceId,
					staging.expandoColumnIds,
					staging.sha256HexId,
					staging.modifiedDate,
					staging.name,
					staging.organizationId,
					staging.parentOrganizationId,
					staging.projectId,
					staging.treePath,
					staging.organizationType
				)
		]]>
	</entry>
	<entry key="RoleMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.role` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					role.*,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(role.fields)
						WHERE
							name = 'name'
					) AS name,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(role.fields)
						WHERE
							name = 'roleId'
					) AS roleId,
					TO_HEX(
						SHA256(
							CONCAT(role.projectId, '#', role.dataSourceId, '#', role.classPK)
						)
					) AS sha256HexId
				FROM
					`{projectId}.dxpentity` AS role
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS roleId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = role.type AND
					analyticsDeleteMessage.dataSourceId = role.dataSourceId AND
					analyticsDeleteMessage.projectId = role.projectId AND
					analyticsDeleteMessage.roleId = role.classPK
				WHERE
					(
						analyticsDeleteMessage.roleId IS NOT NULL OR
						role.uploadDate >= '{previousRunDateString}'
					) AND
					role.type = 'com.liferay.portal.kernel.model.Role'
			) AS staging
			ON
				staging.classPK = replica.roleId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`dataSourceId`,
					`id`,
					`modifiedDate`,
					`name`,
					`projectId`,
					`roleId`
				)
				VALUES (
					staging.dataSourceId,
					staging.sha256HexId,
					staging.modifiedDate,
					staging.name,
					staging.projectId,
					staging.roleId
				)
		]]>
	</entry>
	<entry key="TeamMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.team` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					team.*,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(team.fields)
						WHERE
							name = 'groupId'
					) AS groupId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(team.fields)
						WHERE
							name = 'name'
					) AS name,
					TO_HEX(
						SHA256(
							CONCAT(team.projectId, '#', team.dataSourceId, '#', team.classPK)
						)
					) AS sha256HexId,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(team.fields)
						WHERE
							name = 'teamId'
					) AS teamId
				FROM
					`{projectId}.dxpentity` AS team
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS teamId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = team.type AND
					analyticsDeleteMessage.dataSourceId = team.dataSourceId AND
					analyticsDeleteMessage.projectId = team.projectId AND
					analyticsDeleteMessage.teamId = team.classPK
				WHERE
					(
						analyticsDeleteMessage.teamId IS NOT NULL OR
						team.uploadDate >= '{previousRunDateString}'
					) AND
					team.type = 'com.liferay.portal.kernel.model.Team'
			) AS staging
			ON
				staging.classPK = replica.teamId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.groupId = staging.groupId,
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`dataSourceId`,
					`groupId`,
					`id`,
					`modifiedDate`,
					`name`,
					`projectId`,
					`teamId`
				)
				VALUES (
					staging.dataSourceId,
					staging.groupId,
					staging.sha256HexId,
					staging.modifiedDate,
					staging.name,
					staging.projectId,
					staging.teamId
				)
		]]>
	</entry>
	<entry key="UserGroupMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.usergroup` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					userGroup.*,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(userGroup.fields)
						WHERE
							name = 'name'
					) AS name,
					TO_HEX(
						SHA256(
							CONCAT(userGroup.projectId, '#', userGroup.dataSourceId, '#', userGroup.classPK)
						)
					) AS sha256HexId,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(userGroup.fields)
						WHERE
							name = 'userGroupId'
					) AS userGroupId
				FROM
					`{projectId}.dxpentity` AS userGroup
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS userGroupId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = userGroup.type AND
					analyticsDeleteMessage.dataSourceId = userGroup.dataSourceId AND
					analyticsDeleteMessage.projectId = userGroup.projectId AND
					analyticsDeleteMessage.userGroupId = userGroup.classPK
				WHERE
					(
						analyticsDeleteMessage.userGroupId IS NOT NULL OR
						userGroup.uploadDate >= '{previousRunDateString}'
					) AND
					userGroup.type = 'com.liferay.portal.kernel.model.UserGroup'
			) AS staging
			ON
				staging.classPK = replica.userGroupId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.modifiedDate = staging.modifiedDate,
					replica.name = staging.name
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`dataSourceId`,
					`id`,
					`modifiedDate`,
					`name`,
					`projectId`,
					`userGroupId`
				)
				VALUES (
					staging.dataSourceId,
					staging.sha256HexId,
					staging.modifiedDate,
					staging.name,
					staging.projectId,
					staging.userGroupId
				)
		]]>
	</entry>
	<entry key="UserExpandoColumnMergeStatement">
				<![CDATA[
			MERGE INTO
				`{projectId}.user` AS replica
			USING (
				SELECT
					user.*,
					ARRAY(SELECT unnestExpandoColumnIds
							FROM UNNEST(user.expandoColumnIds) AS unnestExpandoColumnIds
							WHERE unnestExpandoColumnIds <> expandoColumnAnalyticsDeleteMessage.columnId) AS updatedExpandoColumnIds
					FROM (
						SELECT
							*,
							(
								SELECT
									SAFE_CAST(value AS STRING)
								FROM
									UNNEST(fields)
								WHERE
									name = 'classPK'
							) AS columnId,
							(
								SELECT
									SAFE_CAST(value AS STRING)
								FROM
									UNNEST(fields)
								WHERE
									name = 'className'
							) AS className
						FROM
							`{projectId}.dxpentity`
						WHERE
							type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage' AND
							uploadDate >= '{previousRunDateString}'
					) AS expandoColumnAnalyticsDeleteMessage
					INNER JOIN (
						SELECT
							*,
							(
							SELECT ARRAY(
								SELECT
									DISTINCT SAFE_CAST(columnId AS STRING)
								FROM
									UNNEST(expandoFields)
							)
						) AS expandoColumnIds,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'userId'
						) AS userId
						FROM
							`{projectId}.dxpentity`
						WHERE
							type = 'com.liferay.portal.kernel.model.User'
					) AS user
					ON
						expandoColumnAnalyticsDeleteMessage.columnId in UNNEST(user.expandoColumnIds) AND
						expandoColumnAnalyticsDeleteMessage.dataSourceId = user.dataSourceId AND
						expandoColumnAnalyticsDeleteMessage.projectId = user.projectId
					WHERE
						expandoColumnAnalyticsDeleteMessage.className = 'com.liferay.expando.kernel.model.ExpandoColumn'
			) AS staging
			ON
				staging.dataSourceId = replica.dataSourceId AND
				staging.modifiedDate = replica.modifiedDate AND
				staging.projectId = replica.projectId AND
				staging.userId = replica.dxpUserId
			WHEN MATCHED THEN
				UPDATE SET
					replica.expandoColumnIds = staging.updatedExpandoColumnIds
		]]>
	</entry>
	<entry key="UserMergeStatement">
		<![CDATA[
			MERGE INTO
				`{projectId}.user` AS replica
			USING (
				SELECT
					analyticsDeleteMessage.deleted,
					user.*,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'accountId'
					) AS accountId,
					(
						SELECT
							SAFE_CAST(TIMESTAMP_MILLIS(CAST(value AS INT64)) AS TIMESTAMP)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'birthday'
					) AS birthday,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'classNameId'
					) AS classNameId,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'contactId'
					) AS contactId,
					(
						SELECT
							SAFE_CAST(TIMESTAMP_MILLIS(CAST(value AS INT64)) AS TIMESTAMP)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'createDate'
					) AS createDate,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'emailAddress'
					) AS emailAddress,
					(
						SELECT ARRAY(
							SELECT
								DISTINCT SAFE_CAST(columnId AS STRING)
							FROM
								UNNEST(user.expandoFields)
						)
					) AS expandoColumnIds,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'firstName'
					) AS firstName,
					(
						SELECT
							ARRAY (
								SELECT
									SAFE_CAST(number AS INT64)
								FROM
									UNNEST(arrayField.groupIds) AS number
							)
						FROM (
							SELECT (
								SELECT
									JSON_EXTRACT_ARRAY(value)
								FROM
									UNNEST(user.fields)
								WHERE
									name = 'groupIds'
							) groupIds
						) arrayField
					) AS groupIds,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'jobTitle'
					) AS jobTitle,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'languageId'
					) AS languageId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'lastName'
					) AS lastName,
					(
						SELECT
							SAFE_CAST(CAST(value AS STRING) AS BOOL)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'male'
					) AS male,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'middleName'
					) AS middleName,
					(
						SELECT
							ARRAY (
								SELECT
									SAFE_CAST(number AS INT64)
								FROM
									UNNEST(arrayField.organizationIds) AS number
							)
						FROM (
							SELECT (
								SELECT
									JSON_EXTRACT_ARRAY(value)
								FROM
									UNNEST(user.fields)
								WHERE
									name = 'organizationIds'
							) organizationIds
						) arrayField
					) AS organizationIds,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'parentContactId'
					) AS parentContactId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'prefixId'
					) AS prefixId,
					(
						SELECT
							ARRAY (
								SELECT
									SAFE_CAST(number AS INT64)
								FROM
									UNNEST(arrayField.roleIds) AS number
							)
						FROM (
							SELECT (
								SELECT
									JSON_EXTRACT_ARRAY(value)
								FROM
									UNNEST(user.fields)
								WHERE
									name = 'roleIds'
							) roleIds
						) arrayField
					) AS roleIds,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'screenName'
					) AS screenName,
					TO_HEX(
						SHA256(
							CONCAT(user.projectId, '#', user.dataSourceId, '#', user.classPK)
						)
					) AS sha256HexId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'suffixId'
					) AS suffixId,
					(
						SELECT
							ARRAY (
								SELECT
									SAFE_CAST(number AS INT64)
								FROM
									UNNEST(arrayField.teamIds) AS number
							)
						FROM (
							SELECT (
								SELECT
									JSON_EXTRACT_ARRAY(value)
								FROM
									UNNEST(user.fields)
								WHERE
									name = 'teamIds'
							) teamIds
						) arrayField
					) AS teamIds,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'timeZoneId'
					) AS timeZoneId,
					(
						SELECT
							ARRAY (
								SELECT
									SAFE_CAST(number AS INT64)
								FROM
									UNNEST(arrayField.userGroupIds) AS number
							)
						FROM (
							SELECT (
								SELECT
									JSON_EXTRACT_ARRAY(value)
								FROM
									UNNEST(user.fields)
								WHERE
									name = 'userGroupIds'
							) userGroupIds
						) arrayField
					) AS userGroupIds,
					(
						SELECT
							SAFE_CAST(value AS INT64)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'userId'
					) AS userId,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'userName'
					) AS userName,
					(
						SELECT
							SAFE_CAST(value AS STRING)
						FROM
							UNNEST(user.fields)
						WHERE
							name = 'uuid'
					) AS uuid
				FROM
					`{projectId}.dxpentity` AS user
				LEFT JOIN (
					SELECT
						*,
						TRUE AS deleted,
						(
							SELECT
								SAFE_CAST(value AS INT64)
							FROM
								UNNEST(fields)
							WHERE
								name = 'classPK'
						) AS userId,
						(
							SELECT
								SAFE_CAST(value AS STRING)
							FROM
								UNNEST(fields)
							WHERE
								name = 'className'
						) AS className
					FROM
						`{projectId}.dxpentity`
					WHERE
						type = 'com.liferay.analytics.message.storage.model.AnalyticsDeleteMessage'
				) AS analyticsDeleteMessage
				ON
					analyticsDeleteMessage.className = user.type AND
					analyticsDeleteMessage.dataSourceId = user.dataSourceId AND
					analyticsDeleteMessage.projectId = user.projectId AND
					analyticsDeleteMessage.userId = user.classPK
				WHERE
					(
						analyticsDeleteMessage.userId IS NOT NULL OR
						user.uploadDate >= '{previousRunDateString}'
					) AND
					user.type = 'com.liferay.portal.kernel.model.User'
			) AS staging
			ON
				SAFE_CAST(staging.classPK AS INT64) = replica.dxpUserId AND
				staging.dataSourceId = replica.dataSourceId AND
				staging.projectId = replica.projectId
			WHEN MATCHED AND staging.deleted IS NULL THEN
				UPDATE SET
					replica.accountId = staging.accountId,
					replica.birthday = staging.birthday,
					replica.emailAddress = staging.emailAddress,
					replica.emailAddressHashed = TO_HEX(SHA256(staging.emailAddress)),
					replica.expandoColumnIds = staging.expandoColumnIds,
					replica.firstName = staging.firstName,
					replica.groupIds = staging.groupIds,
					replica.jobTitle = staging.jobTitle,
					replica.languageId = staging.languageId,
					replica.lastName = staging.lastName,
					replica.male = staging.male,
					replica.middleName = staging.middleName,
					replica.modifiedDate = staging.modifiedDate,
					replica.organizationIds = staging.organizationIds,
					replica.parentContactId = staging.parentContactId,
					replica.prefixId = staging.prefixId,
					replica.roleIds = staging.roleIds,
					replica.screenName = staging.screenName,
					replica.suffixId = staging.suffixId,
					replica.teamIds = staging.teamIds,
					replica.timeZoneId = staging.timeZoneId,
					replica.userGroupIds = staging.userGroupIds,
					replica.userName = staging.userName
			WHEN MATCHED AND staging.deleted = true THEN
				DELETE
			WHEN NOT MATCHED BY TARGET AND staging.deleted IS NULL THEN
				INSERT (
					`accountId`,
					`birthday`,
					`classNameId`,
					`classPK`,
					`contactId`,
					`createDate`,
					`dataSourceId`,
					`dxpUserId`,
					`emailAddress`,
					`emailAddressHashed`,
					`expandoColumnIds`,
					`firstName`,
					`groupIds`,
					`id`,
					`jobTitle`,
					`languageId`,
					`lastName`,
					`male`,
					`middleName`,
					`modifiedDate`,
					`organizationIds`,
					`parentContactId`,
					`prefixId`,
					`projectId`,
					`roleIds`,
					`screenName`,
					`suffixId`,
					`teamIds`,
					`timeZoneId`,
					`userGroupIds`,
					`userName`,
					`uuid`
				)
				VALUES (
					staging.accountId,
					staging.birthday,
					staging.classNameId,
					staging.classPK,
					staging.contactId,
					staging.createDate,
					staging.dataSourceId,
					staging.userId,
					staging.emailAddress,
					TO_HEX(SHA256(staging.emailAddress)),
					staging.expandoColumnIds,
					staging.firstName,
					staging.groupIds,
					staging.sha256HexId,
					staging.jobTitle,
					staging.languageId,
					staging.lastName,
					staging.male,
					staging.middleName,
					staging.modifiedDate,
					staging.organizationIds,
					staging.parentContactId,
					staging.prefixId,
					staging.projectId,
					staging.roleIds,
					staging.screenName,
					staging.suffixId,
					staging.teamIds,
					staging.timeZoneId,
					staging.userGroupIds,
					staging.userId,
					staging.userName,
					staging.uuid
				)
		]]>
	</entry>
</properties>