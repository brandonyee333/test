40a41,42
> [[ -f /usr/share/elasticsearch/config/elasticsearch.keystore ]] || (run_as_other_user_if_needed elasticsearch-keystore create)
> 
59d60
<     [[ -f /usr/share/elasticsearch/config/elasticsearch.keystore ]] || (run_as_other_user_if_needed elasticsearch-keystore create)
75a77,82
> if [[ -n "$GCS_CREDENTIALS" ]]; then
>   if ! (run_as_other_user_if_needed elasticsearch-keystore list | grep -q '^gcs.client.default.credentials_file$'); then
>     (run_as_other_user_if_needed echo $GCS_CREDENTIALS | /usr/share/elasticsearch/bin/elasticsearch-keystore add -x gcs.client.default.credentials_file)
>   fi
> fi
> 
83c96
< run_as_other_user_if_needed /usr/share/elasticsearch/bin/elasticsearch <<<"$KEYSTORE_PASSWORD"
---
> run_as_other_user_if_needed /usr/share/elasticsearch/bin/elasticsearch <<<"$KEYSTORE_PASSWORD"
\ No newline at end of file