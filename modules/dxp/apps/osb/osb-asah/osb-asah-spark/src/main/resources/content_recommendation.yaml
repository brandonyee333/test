aws:
    access:
        key:
            id: $AWS_ACCESS_KEY_ID
    secret:
        access:
            key: $AWS_SECRET_ACCESS_KEY
    storage:
        path: s3a://analytics-cloud-aws-personalize
google:
    storage:
        path: gs://analytics-cloud-analytics-events-uat
spark:
    jars:
        packages: "org.apache.hadoop:hadoop-aws:2.7.3"