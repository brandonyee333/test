			SELECT
				Group_.companyId,
				Group_.groupId,
				DLFileEntryType.name
			FROM
				DLFileEntryType, Group_ 
			WHERE
				Group_.groupId = DLFileEntryType.groupId AND
				(
					( Group_.companyId = ?) AND
					( Group_.groupId = ? ) AND
					(
						(lower(DLFileEntryType.name) LIKE ? ) AND
						(DLFileEntryType.description LIKE ? )
					)
				)