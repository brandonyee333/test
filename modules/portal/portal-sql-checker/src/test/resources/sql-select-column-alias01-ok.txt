SELECT
	Group_.companyId as companId,
	DLFileEntryType.groupId as groupId,
	DLFileEntryType.name as name
FROM
	DLFileEntryType inner join Group_ on DLFileEntryType.groupId = Group_.groupId
ORDER BY
	name ASC