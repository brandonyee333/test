definition {

	@summary = "Default summary"
	macro addCP(initialURL = null) {
		IFrame.selectConfigurationFrame();

		Type(
			locator1 = "TextInput#INITIAL_URL",
			value1 = ${initialURL});

		PortletEntry.save();

		SelectFrame(value1 = "relative=top");
	}

}