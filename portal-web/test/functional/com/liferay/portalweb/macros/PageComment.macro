definition {

	@summary = "Default summary"
	macro addWithWorkflowPG(commentAuthor = null, commentBody = null) {
		PageComment.reply(commentBody = ${commentBody});

		var key_commentBody = ${commentBody};

		AssertTextEquals.assertPartialText(
			locator1 = "Message#SUCCESS",
			value1 = "Your request completed successfully.");

		AssertElementPresent(locator1 = "Comments#WORKFLOW_STATUS");

		if (IsTextEqual(locator1 = "Comments#WORKFLOW_STATUS", value1 = "Pending (Review)")) {
			AssertTextEquals(
				locator1 = "Comments#WORKFLOW_STATUS",
				value1 = "Pending (Review)");
		}
		else {
			AssertTextEquals(
				locator1 = "Comments#WORKFLOW_STATUS",
				value1 = "Pending (Created)");
		}

		var key_entryComment = ${commentBody};

		AssertTextEquals(
			locator1 = "Comments#COMMENT_BODY_SPECIFIC",
			value1 = ${commentBody});

		AssertTextEquals(
			locator1 = "Comments#COMMENT_AUTHOR",
			value1 = ${commentAuthor});
	}

	@summary = "Default summary"
	macro editCommentField(commentBody = null) {
		Type.typeEditor(
			locator1 = "Comments#COMMENT_FIELD",
			value1 = ${commentBody});
	}

	@summary = "Default summary"
	macro reply(commentBody = null) {
		PageComment.editCommentField(commentBody = ${commentBody});

		AssertClick(
			locator1 = "Button#REPLY",
			value1 = "Reply");
	}

	@summary = "Default summary"
	macro tearDownPG(pageName = null) {
		Navigator.gotoPage(pageName = ${pageName});

		while (IsElementPresent(locator1 = "Icon#BODY_VERTICAL_ELLIPSIS")) {
			LexiconEntry.gotoEllipsisMenuItemNoError(menuItem = "Delete");

			Confirm(value1 = "Are you sure you want to delete this? It will be deleted immediately.");

			AssertTextEquals.assertPartialText(
				locator1 = "Message#SUCCESS",
				value1 = "Your request completed successfully.");
		}
	}

	@summary = "Default summary"
	macro viewAsWorkflowAsset(commentBody = null) {
		SiteAdmin.viewPortletTitle(portletTitle = ${commentBody});

		var key_commentBody = ${commentBody};

		AssertTextEquals(
			locator1 = "Comments#COMMENT_BODY_ASSET",
			value1 = ${commentBody});
	}

	@summary = "Default summary"
	macro viewPG(commentAuthor = null, commentBody = null) {
		var key_commentBody = ${commentBody};

		AssertElementNotPresent(locator1 = "Comments#WORKFLOW_STATUS");

		var key_entryComment = ${commentBody};

		AssertTextEquals(
			locator1 = "Comments#COMMENT_BODY_SPECIFIC",
			value1 = ${commentBody});

		AssertTextEquals(
			locator1 = "Comments#COMMENT_AUTHOR",
			value1 = ${commentAuthor});
	}

	@summary = "Default summary"
	macro viewWithWorkflowPG(commentAuthor = null, commentBody = null, commentStatus = null) {
		var key_commentBody = ${commentBody};

		AssertTextEquals(
			locator1 = "Comments#WORKFLOW_STATUS",
			value1 = ${commentStatus});

		var key_entryComment = ${commentBody};

		AssertTextEquals(
			locator1 = "Comments#COMMENT_BODY_SPECIFIC",
			value1 = ${commentBody});

		AssertTextEquals(
			locator1 = "Comments#COMMENT_AUTHOR",
			value1 = ${commentAuthor});
	}

}