@default = "rightClick"
definition {

	function rightClick(locator1) {
		WaitForSPARefresh();

		

		selenium.mouseOver(${locator1});

		selenium.rightClick(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}