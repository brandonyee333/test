@default = "isNotChecked"
definition {

	function isNotChecked(locator1) {
		WaitForSPARefresh();

		

		

		selenium.isNotChecked(${locator1});
	}

	function isNotCheckedNotVisible(locator1) {
		WaitForSPARefresh();

		

		selenium.isNotChecked(${locator1});
	}

}