@default = "assertNotSelectedLabel"
definition {

	function assertNotSelectedLabel(locator1, value2, value1) {
		WaitForSPARefresh();

		

		selenium.assertNotSelectedLabel(${value1}, ${value2});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}