@default = "assertTextMatches"
definition {

	function assertTextMatches(locator1, value1) {
		WaitForSPARefresh();

		

		selenium.assertTextMatches(${locator1}, ${value1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}