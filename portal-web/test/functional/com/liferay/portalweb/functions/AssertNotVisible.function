@default = "assertNotVisible"
definition {

	function assertNotVisible(locator1) {
		WaitForSPARefresh();

		

		selenium.assertNotVisible(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}