@default = "select"
definition {

	function select(locator1, value1) {
		WaitForSPARefresh();

		selenium.pause(5000);

		WaitForSPARefresh();

		

		selenium.mouseOver(${locator1});

		selenium.select(${locator1}, ${value1});
	}

}