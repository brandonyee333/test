@default = "tripleClick"
definition {

	function tripleClick(locator1) {
		WaitForSPARefresh();

		

		selenium.mouseOver(${locator1});

		selenium.tripleClick(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}