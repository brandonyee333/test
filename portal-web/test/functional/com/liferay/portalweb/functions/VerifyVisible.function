@default = "verifyVisible"
@summary = "Verify that '${locator1}' is visible"
definition {

	function verifyVisible(locator1) {
		WaitForSPARefresh();

		

		selenium.verifyVisible(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}