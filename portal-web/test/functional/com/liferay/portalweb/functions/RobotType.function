@default = "robotType"
@summary = "Type to '${locator1}' the value '${value1}'"
definition {

	function robotType {
		WaitForSPARefresh();

		selenium.type();
	}

	function robotTypeShortcut {
		WaitForSPARefresh();

		selenium.sendKeys();
	}

}