@component-name = "portal-database-partitioning"
definition {

	property database.partition.enabled = "true";
	property database.types = "mysql";
	property portal.acceptance = "summer";
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "Database Partitioning";
	property timeout.explicit.wait = "180";

	setUp {
		TestCase.setUpPortalInstance();

		User.firstLoginPG();
	}

	tearDown {
		PortalInstances.tearDownCP();
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved
			
			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies2 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies2";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies3 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies3";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies4 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies4";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies5 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies5";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies6 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies6";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies7 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies7";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies8 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies8";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies9 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies9";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies10 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies10";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies11 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies11";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies12 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies12";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies13 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies13";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies14 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies14";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies15 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies15";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies16 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies16";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies17 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies17";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

	@priority = "4"
	test ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies18 {
		property osgi.module.configuration.file.names = "com.liferay.journal.configuration.JournalServiceConfiguration.config";
		property osgi.module.configurations = "checkInterval=I&quot;1&quot;";
		property test.name.skip.portal.instance = "DatabasePartitioning1#ScheduleWebContentChangesWithDBPartitioningActivatedAcrossVariousCompanies18";

		task ("Add a new Company") {
			HeadlessPortalInstanceAPI.addPortalInstance(
				domain = "www.able.com",
				portalInstanceId = "www.able.com",
				virtualHost = "www.able.com");

			User.firstLoginUI(
				password = "test",
				specificURL = "http://www.able.com:8080",
				userEmailAddress = "test@www.able.com");
		}

		task ("Add a WebContent article on new Company") {
			WebContentNavigator.openWebContentAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");

			WebContent.increaseDisplayDate(minuteIncrease = "7");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(
				baseURL = "http://www.able.com:8080",
				siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title New Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title New Company is scheduled and will be displayed");

		task ("Add a WebContent article on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			WebContentNavigator.openWebContentAdmin(siteURLKey = "guest");

			WebContentNavigator.gotoAddCP();

			WebContent.addCP(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");

			WebContent.increaseDisplayDate(minuteIncrease = "2");

			PortletEntry.publish();

			WebContent.getCurrentTime();
		}

		task ("Add a page and WebContent display select a WebContent article") {
			PagesAdmin.openPagesAdmin(siteURLKey = "guest");

			PagesAdmin.addPublicPage(pageName = "Web Content Display Page");

			Navigator.gotoPage(pageName = "Web Content Display Page");

			Portlet.addPG(portletName = "Web Content Display");

			WebContentDisplayPortlet.selectWebContent(webContentTitle = "WC WebContent Title Default Company");

			IFrame.closeFrame();
		}

		WebContent.getCurrentTime();

		AssertTextEquals.assertPartialText(
			key_warningMessage = "WC WebContent Title Default Company is scheduled and will be displayed",
			locator1 = "Message#WARNING_SPECIFIC",
			value1 = "WC WebContent Title Default Company is scheduled and will be displayed");

		task ("View WebContent Article Publish on default Company") {
			Pause(locator1 = "180000");

			// Disable Check on Default company until LPS-116111 is resolved

			Refresh();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}

		task ("New Company publish it's WebContent Article") {
			SignOut.signOut();

			Navigator.gotoPage(
				baseURL = "www.able.com:8080",
				pageName = "Web Content Display Page");

			WebContent.getCurrentTime();

			AssertTextEquals.assertPartialText(
				key_warningMessage = "WC WebContent Title New Company is scheduled and will be displayed",
				locator1 = "Message#WARNING_SPECIFIC",
				value1 = "WC WebContent Title New Company is scheduled and will be displayed");

			Pause(locator1 = "300000");

			Refresh();

			WebContent.getCurrentTime();

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content New Company",
				webContentTitle = "WC WebContent Title New Company");
		}

		// Disable Check on Default company until LPS-116111 is resolved

		task ("WebContent Article not repulish on default Company") {
			SignOut.signOut();

			User.firstLoginPG();

			Navigator.gotoPage(pageName = "Web Content Display Page");

			WebContentDisplayPortlet.viewContent(
				webContentContent = "WC WebContent Content Default Company",
				webContentTitle = "WC WebContent Title Default Company");
		}
	}

}